import React from 'react';
import { useStaticQuery, graphql } from 'gatsby'
import Img from 'gatsby-image'

const About = () => {

    return (
        <section className="sec">
            <p className="p">Annika, 27, dies das</p>
            <i className="fas fa-bars"></i>
        </section>
    )
}

export default About